最大抓包数可以自己设置
一次捕获数据包的最大byte数
是否采用混乱模式
捕获的数据包超时设置(数量级为毫秒级)
设置捕获过滤器(未知)

设置网卡界面采用绝对布局
给列表加一个序号
重写JButton组件

先搞多线程
再搞数据存储（无法实现。。。）

网卡
    网卡描述
    网卡的MAC地址
    网卡的IP地址数组（多个）

包
    UDP
    源IP地址
    目标IP地址
    源端口号
    目标端口号
    偏移量（目前不知道干啥的）
    数据

    TCP
    源IP地址
    目标IP地址
    源端口号
    目标端口号
    偏移量（目前不知道干啥的）
    数据

    ARP
    目标MAC地址
    源MAC地址



TCP UDP
ICMP IGMP
IP
    ARP
Ethernet


输入值检查setValue()                            完成
调整主界面默认窗口大小与wires hark一致           完成
添加第三显示框组件                              完成
添加抓包完成后的统计窗口                         未完成